providers:
  nvidia_nim:
    primary: stepfun-ai/step-3.7-flash
    fallback: stepfun-ai/step-3.7-flash
    base_url: https://integrate.api.nvidia.com/v1
    api_key_env: NVIDIA_API_KEY

rate_limits:
  global:
    requests_per_minute: 35
    window_seconds: 60
  per_job:
    requests_per_minute: 30
    window_seconds: 60

cache:
  database_path: storage/cache.db
  enabled: true
